Commands in Git

  1.git init
    Initialize a new Git repository in the current folder.

  2. git clone url
     Create a local copy of a remote repository.

  3.git status
    Check the current status of files (staged, modified, untracked).

  4.git add .
    Stage all changes in the current directory.

  5.git commit -m "message"
    Save staged changes with a descriptive message.

  6.git push
    Send local commits to the remote repository.

  7.git pull
    Fetch and merge changes from the remote repository.
  
  8.git branch
    Creates a new branch for separate work.

  9.git checkout 
    Switches to the specified branch.

  10.git merge 
     Combines another branch into the current one.

  11.git reset, git checkout -- 
     Used to unstage or discard local changes.

  12.git remote add origin url
     Connect your local repo to a remote one.
